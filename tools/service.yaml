apiVersion: v1
kind: Service
metadata:
  name: kube-state-metrics-service
  namespace: kube-system
spec:
  ports:
    - protocol: TCP
      targetPort: 8080
      port: 8080
  selector:
    app: kube-state-metrics
  type: ClusterIP